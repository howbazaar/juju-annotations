Juju plugin for dealing with annotations
========================================

First attempt using python-libjuju.

Trying to make this a plugin too.


The plugin requires python-libjuju in the python path.